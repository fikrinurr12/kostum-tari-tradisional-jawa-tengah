# requirements.txt
# Versi dipin secara spesifik untuk menghindari konflik dependency saat
# deploy di Streamlit Community Cloud.
#
# CATATAN PENTING:
# - Memakai `tensorflow-cpu` (bukan `tensorflow` penuh) karena Streamlit
#   Cloud hanya menyediakan CPU, sehingga versi CPU-only ini jauh lebih
#   ringan untuk diinstall dan tidak mencoba mencari driver GPU/CUDA
#   yang tidak ada di environment tersebut.
# - Versi TensorFlow 2.15.x dipilih agar sama dengan versi yang dipakai
#   saat training di notebook (Google Colab), supaya format model
#   (.h5/.keras) terbaca tanpa masalah kompatibilitas versi.
# - Wajib pin Python ke 3.11 lewat runtime.txt (lihat file tersebut),
#   karena TensorFlow 2.15 belum tersedia untuk Python 3.12/3.13.

# streamlit==1.36.0
# tensorflow-cpu==2.15.0
# numpy==1.26.4
# pillow==10.3.0

streamlit==1.36.0
tensorflow-cpu==2.20.0
numpy>=1.26.0
pillow==10.3.0
