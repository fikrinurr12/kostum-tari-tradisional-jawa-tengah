# requirements.txt — TariJateng
# Klasifikasi Kostum Tari Tradisional Jawa Tengah
#
# CATATAN DEPLOYMENT:
# - Gunakan tensorflow-cpu (bukan tensorflow penuh) untuk server tanpa GPU
# - Sesuaikan versi tensorflow-cpu dengan versi yang digunakan saat training
#   (cek: import tensorflow as tf; print(tf.__version__) di notebook Google Colab)
# - Set Python versi secara manual di Advanced Settings saat deploy ke
#   Streamlit Community Cloud → pilih Python 3.11
# - Fitur deteksi gambar di luar cakupan menggunakan MSP (output model saja),
#   tidak memerlukan library computer vision tambahan seperti OpenCV.
# - Deteksi selfie memakai opencv-python-headless (Haar Cascade), varian
#   headless dipakai karena server tidak punya display/GUI.

streamlit==1.36.0
tensorflow-cpu==2.20.0
numpy>=1.26.0
pillow==10.3.0
matplotlib>=3.8.0
opencv-python-headless==4.13.0.92
